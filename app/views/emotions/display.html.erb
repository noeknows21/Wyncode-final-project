<%= stylesheet_link_tag params[:controller] %>

<section id="top-header"></section>


<h2> <%=current_user.name%>'s videos</h2>
  <% @current_user_videos.each_with_index do |v, index| %>
    <a href='<%="https://s3.amazonaws.com/pitchusers/#{v}"%>' class="videos-titles"> <%= "VIDEO " + (index + 1).to_s %> </a> <br>
    <br>
    <br>

    <a href='<%="/emotions/graph/#{v.gsub(/.mp4/, "")}"%>' class="emotions-titles"> <%= "Obtain Emotion Analysis" %> </a> <br>
    <br>
    <br>

  <%end%>

<section id="bottom-footer"></section>
